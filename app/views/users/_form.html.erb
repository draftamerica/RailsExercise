<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with model: @user do |form| %>
      <div>
          <div class="data">
              <%= label :username, "username", id: "username", class: "data-label" %>
              <%= form.text_field :username, id: "username", class: "data-value" %>
          </div>
          <div class="data">
              <%= label :password, "password", id: "password", class: "data-label" %>
              <%= form.text_field :password, id: "password", class: "data-value" %>
          </div>
          <div class="data">
              <%= form.submit %>
          </div>
      </div>
  <% end %>
<% end %>
